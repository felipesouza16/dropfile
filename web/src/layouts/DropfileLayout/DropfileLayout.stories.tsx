import type { Meta, StoryObj } from '@storybook/react'

import DropfileLayout from './DropfileLayout'

const meta: Meta<typeof DropfileLayout> = {
  component: DropfileLayout,
}

export default meta

type Story = StoryObj<typeof DropfileLayout>

export const Primary: Story = {}
